# ===================================================
# IngestIQ Environment Configuration (Example)
# ===================================================

# --- OpenAI Credentials ---
OPENAI_API_KEY=your_openai_api_key
OPENAI_ENDPOINT=https://your-endpoint.openai.azure.com/
OPENAI_API_VERSION=2024-02-15-preview
OPENAI_EMBEDDINGS_MODEL=text-embedding-3-large
OPENAI_CHAT_MODEL=gpt-4

# --- Database Credentials ---
POSTGRES_USER=ingestiq
POSTGRES_PASSWORD=supersecretpassword
POSTGRES_DB=ingestiq_db
DATABASE_URL=postgresql+psycopg2://ingestiq:supersecretpassword@postgres:5432/ingestiq_db

# RabbitMQ Credentials
RABBITMQ_DEFAULT_USER=ingestiq
RABBITMQ_DEFAULT_PASS=supersecretpassword
RABBITMQ_URL=amqp://ingestiq:supersecretpassword@rabbitmq:5672/

# --- Airflow Configuration ---
# Run `echo "$(id -u):$(id -g)"` in your terminal to get the correct value for your system.
AIRFLOW_UID=5000:0

# --- Security ---
# Generate a secure key using `openssl rand -hex 32`
SECRET_KEY=generate_a_secure_key_here
